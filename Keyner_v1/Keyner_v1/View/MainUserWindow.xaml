<Window x:Class="Keyner_v1.View.MainUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Keyner_v1.View"
        mc:Ignorable="d"
        Title="MainUserWindow" Height="450" Width="850" Background="MediumTurquoise" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key = "ButtonStyle" TargetType = "Button">
            <Setter Property = "Foreground" Value = "Black" />
            <Style.Triggers>
                <Trigger Property = "IsMouseOver" Value = "True">
                    <Setter Property="FontSize" Value="30"/>
                    <Setter Property = "Foreground" Value = "Red" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="TBstyle">
            <Setter Property="TextBlock.FontFamily" Value="Comic Sans MS"/>
            <Setter Property="TextBlock.FontSize" Value="35" />
            <Setter Property="TextBlock.FontWeight" Value="Thin"/>
            <Setter Property="TextBlock.Foreground" Value="Black"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <DataGrid Name="datagrid1" Grid.Column="0" Grid.RowSpan="3" Margin="10" IsReadOnly="True" AutoGenerateColumns="False" Background="Lavender" SelectionUnit="FullRow" SelectionMode="Single" CanUserAddRows="False" CanUserDeleteRows="False" CanUserSortColumns="False" SelectionChanged="datagrid1_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Тест" Binding="{Binding TestName}" />
                <DataGridTextColumn Header="Найкращий час" Binding="{Binding BestTime}" />
                <DataGridTemplateColumn Header="Оцінка">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Image Source="{Binding Mark}" Width="60" Height="40" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Час" Width="45" Binding="{Binding Time}" />
                <DataGridTextColumn Header="Помилки" Binding="{Binding Mistakes}" />
                <DataGridCheckBoxColumn Header="Пройдено" Binding="{Binding IsPassed}" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1" Grid.Row="0" VerticalAlignment="Center">
            <TextBlock Name="txt1" Margin="5" HorizontalAlignment="Center" FontSize="35" Style="{StaticResource TBstyle}"></TextBlock>
            <DockPanel>
                <TextBlock Text="Ваші кошти:" Margin="5" Width="180" VerticalAlignment="Center" HorizontalAlignment="Left" FontSize="27" Style="{StaticResource TBstyle}"></TextBlock>
                <TextBlock Name="txt2" Margin="5" Width="50" VerticalAlignment="Center" Style="{StaticResource TBstyle}"></TextBlock>
                <Image Name="MoneyImage" Margin="5" Height="50" Width="50" ></Image>
            </DockPanel>
            <TextBlock Name="txt3" Margin="5" VerticalAlignment="Center" Style="{StaticResource TBstyle}"></TextBlock>
        </StackPanel>

        <Image Name="monster" Grid.Column="1" Grid.Row="1" Margin="10"></Image>
        
        <DockPanel Grid.Column="1" Grid.Row="2" VerticalAlignment="Center" >
            <Button Name="gamebutton" Content="Пройти" FontSize="20" FontWeight="Bold" Width="90" Margin="5"  Background="Crimson" HorizontalAlignment="Left" Style="{StaticResource ButtonStyle}" Click="gamebutton_Click"></Button>
            <Button Name="shopbutton" Content="Магазин"  Margin="5" Width="70" Background="Cyan" FontSize="14" Click="shopbutton_Click"></Button>
            <Button Name="settingbutton" Margin="10"  Width="50" Height="50" Click="settingbutton_Click" >
                <Button.Background>
                    <ImageBrush ImageSource="../Pictures/setting-icon.png"></ImageBrush>
                </Button.Background>
            </Button>
            <Button Name="exitbutton" Content="Вихід" FontSize="14" Margin="5,10" Width="50" Background="Teal" IsDefault="True" Click="exitbutton_Click"></Button>
        </DockPanel>
        
    </Grid>
</Window>
